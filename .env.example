# NEVER commit .env (only .env.example)

# App / Project
ENV=dev
LOG_LEVEL=INFO

# Database (se usar)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=app

# AWS (se usar)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=__REPLACE__
AWS_SECRET_ACCESS_KEY=__REPLACE__

# S3 buckets (exemplo)
S3_BUCKET_RAW=__REPLACE__
S3_BUCKET_CURATED=__REPLACE__