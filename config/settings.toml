[app]
name = ""fraud-streaming-pipeline"
env = "dev"
log_level = "INFO"

[storage]
# Exemplo para projetos com lake
raw_path = "data/raw"
curated_path = "data/curated"

[database]
# Usado em projetos DW / metadata
host = "localhost"
port = 5432
db = "app"
user = "app"
password_env = "POSTGRES_PASSWORD"  # nunca coloque senha em TOML